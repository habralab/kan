# Board filter UX screenshots

Screenshots for `habralab/kan` commit `5240798c` using the synthetic
`Defense of Helm's Deep` fixture. The screenshots contain no production data.

- `00-filter-groups-desktop-dark.png` — active counts per filter group and the
  explicit clear action.
- `01-selected-value-desktop-dark.png` — selected value, back navigation and
  the clear action inside a group.
- `02-filter-groups-mobile-dark.png` — the same group summary at a mobile
  viewport.
